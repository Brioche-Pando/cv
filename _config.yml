image: node:latest

stages:
  - build

pages:
  stage: build
  script:
    - npm install
    - npx resumed render resume.json --theme stackoverflow
    - mv resume.html index.html

  artifacts:
    paths:
      - public
